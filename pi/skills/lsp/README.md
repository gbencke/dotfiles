# LSP Skill

This skill configuration defines the metadata and instructions for the `lsp` capability within the Pi AI Coding Agent.

It tells the agent how to perform structural code analysis, navigation, and diagnostics using `ast-grep` and `typescript-language-server`.

## Related Extension
The actual implementation of this skill's tools is located in `../../extensions/lsp.ts`.
